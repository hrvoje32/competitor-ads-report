import Link from "next/link"; import { prisma } from "@/lib/prisma"; import { EmptyState, PageHeader } from "../components"; import { deleteBrand } from "./actions";
export const dynamic = "force-dynamic";
export default async function BrandsPage() { const brands = await prisma.brand.findMany({ include: { googleAdvertisers: true, metaPages: true }, orderBy: [{ sortOrder: "asc" }, { name: "asc" }] }); return <><PageHeader title="Brands" description="Manage the automotive brands included in reports." action={<Link href="/brands/new" className="button">Add brand</Link>}/>{brands.length ? <div className="card overflow-hidden"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Brand</th><th className="p-4">Country</th><th className="p-4">Sources</th><th className="p-4">Order</th><th className="p-4"/></tr></thead><tbody>{brands.map(b => <tr className="border-t border-slate-100" key={b.id}><td className="p-4 font-semibold">{b.name}</td><td className="p-4">{b.countryCode}</td><td className="p-4 text-slate-500">{b.googleAdvertisers.length} Google · {b.metaPages.length} Meta</td><td className="p-4">{b.sortOrder}</td><td className="p-4 text-right"><Link className="mr-4 font-semibold text-blue-700" href={`/brands/${b.id}/edit`}>Edit</Link><form className="inline" action={deleteBrand.bind(null,b.id)}><button className="font-semibold text-rose-700">Delete</button></form></td></tr>)}</tbody></table></div> : <EmptyState title="No brands" detail="Add brands before creating a monthly report." action={<Link className="button" href="/brands/new">Add brand</Link>}/>}</> }
