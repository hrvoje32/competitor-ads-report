import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { BackLink, PageHeader } from "../../../components"; import { updateBrand } from "../../actions"; import { BrandForm } from "../../brand-form";
export default async function EditBrandPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const brand = await prisma.brand.findUnique({ where: { id }, include: { googleAdvertisers: { orderBy: { advertiserId: "asc" } }, metaPages: { orderBy: { pageId: "asc" } } } }); if (!brand) notFound(); return <><BackLink href="/brands">Brands</BackLink><PageHeader title={`Edit ${brand.name}`}/><BrandForm brand={brand} action={updateBrand.bind(null,id)}/></> }
