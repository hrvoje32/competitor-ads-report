import { Brand } from "@/generated/prisma/client";
import AdvertiserIdField from "./advertiser-id-field";
export function BrandForm({ brand, action }: { brand?: Brand; action: (formData: FormData) => void | Promise<void> }) { const v = (key: keyof Brand) => brand?.[key] as string | number | null | undefined; return <form action={action} encType="multipart/form-data" className="card max-w-3xl p-6"><div className="grid gap-5 md:grid-cols-2"><label><span className="label">Brand name *</span><input name="name" defaultValue={v("name") ?? ""} required /></label><label><span className="label">Logo</span><input name="logo" type="file" accept="image/*" /><span className="mt-1 block text-xs text-slate-400">Optional; optimized and saved locally.</span></label><label><span className="label">Website</span><input name="websiteUrl" type="url" defaultValue={v("websiteUrl") ?? ""} placeholder="https://" /></label><label><span className="label">Google Ads Transparency URL</span><input name="googleTransparencyUrl" type="url" defaultValue={v("googleTransparencyUrl") ?? ""} placeholder="https://" /></label><AdvertiserIdField brandName={String(v("name") ?? "")} initialValue={String(v("googleAdvertiserId") ?? "")}/><label><span className="label">Meta Ad Library URL</span><input name="metaAdLibraryUrl" type="url" defaultValue={v("metaAdLibraryUrl") ?? ""} placeholder="https://" /></label><label><span className="label">Meta Page ID</span><input name="metaPageId" defaultValue={v("metaPageId") ?? ""} /></label><label><span className="label">Country</span><input name="countryCode" maxLength={2} defaultValue={v("countryCode") ?? "HR"} required /></label><label><span className="label">Sort order</span><input name="sortOrder" type="number" defaultValue={v("sortOrder") ?? 0} /></label></div><div className="mt-7"><button className="button">{brand ? "Save changes" : "Add brand"}</button></div></form>; }
