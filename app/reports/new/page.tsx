import { prisma } from "@/lib/prisma"; import { BackLink, EmptyState, PageHeader } from "../../components"; import { createReport } from "../actions"; import { months } from "@/lib/utils";
export const dynamic = "force-dynamic";
export default async function NewReportPage() { const brands = await prisma.brand.findMany({ orderBy: [{ sortOrder: "asc" }, { name: "asc" }] }); const now = new Date(); if (!brands.length) return <><BackLink href="/reports">Reports</BackLink><PageHeader title="New monthly report"/><EmptyState title="Add brands first" detail="A report needs at least one tracked brand."/></>; return <><BackLink href="/reports">Reports</BackLink><PageHeader title="New monthly report" description="Choose the period and competitor brands to review."/><form action={createReport} className="card max-w-3xl p-6"><div className="grid gap-5 md:grid-cols-2"><label className="md:col-span-2"><span className="label">Title *</span><input name="title" required defaultValue={`${months[now.getMonth()]} ${now.getFullYear()} competitor advertising report`} /></label><label><span className="label">Month *</span><select name="month" defaultValue={now.getMonth() + 1}>{months.map((m,i)=><option key={m} value={i+1}>{m}</option>)}</select></label><label><span className="label">Year *</span><input name="year" type="number" min="2020" max="2100" defaultValue={now.getFullYear()} required /></label><label><span className="label">Country *</span><input name="countryCode" maxLength={2} defaultValue="HR" required /></label></div><fieldset className="mt-7"><legend className="label">Include brands *</legend><p className="mb-3 text-sm text-slate-500">Select the competitors to include in this report.</p><div className="grid gap-3 sm:grid-cols-2">{brands.map(b=><label className="brand-option flex min-h-12 cursor-pointer items-center rounded-lg border border-slate-200 bg-white px-4 py-3 transition hover:border-teal-500 hover:bg-teal-50" key={b.id}><input type="checkbox" name="brandIds" value={b.id} defaultChecked/><span className="ml-3 font-medium text-slate-800">{b.name}</span></label>)}</div></fieldset><button className="button mt-7">Create report</button></form></> }
